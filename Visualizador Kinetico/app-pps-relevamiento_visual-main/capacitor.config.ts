import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.visualizador kinetico.app',
  appName: 'visualizador kinetico',
  webDir: 'www'
};

export default config;
