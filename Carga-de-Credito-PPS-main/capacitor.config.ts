import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cargacreditopps.app',
  appName: 'Carga de Credito',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
